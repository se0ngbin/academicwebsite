---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <h1>Contact</h1>
  <p class="intro" style="line-height: 1.8; font-size: 1.2rem;">Have a question or want to collaborate? I'd love to hear from you! </p>
  <p style="line-height: 1.8; font-size: 1.2rem;">Please email me at <strong>parkseongbin [at] ucla [dot] edu.</strong></p>
  {/*
  <h2>Prospective Students</h2>
  <p style="line-height: 1.8; font-size: 1.2rem;">
    If you are a student at UCLA interested collaborating on a project, contact me directly or reach out to my advisor. <a href="https://ssysarch.ee.ucla.edu/join.html" target="_blank">Here</a> is more information.
  </p>
  */}
  <p style="line-height: 1.8; font-size: 1.2rem;">
    I generally respond to emails within 2 business days, so if you don't hear back within a week, please feel free to follow up.
    If you are reaching out regarding a specific publication, please include the paper title in the subject line.
  </p>

{/*
  <div class="contact-card">
    <!-- BAREBONES MODE: simple POST form (no client JS). Replace {YOUR_FORMSPREE_ID} with your Formspree ID -->
    <form action="https://formspree.io/f/{YOUR_FORMSPREE_ID}" method="POST">
      <div class="form-group">
        <label for="name">Name</label>
        <input type="text" id="name" name="name" placeholder="Your Name" required />
      </div>
      <div class="form-group">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" placeholder="your.email@example.com" required />
      </div>
      <div class="form-group">
        <label for="message">Message</label>
        <textarea id="message" name="message" rows="6" placeholder="Your message..." required></textarea>
      </div>
      <button type="submit" class="submit-btn">Send Message</button>
    </form>
    <!-- To restore the JS/sendgrid flow, revert to the previous form and client script (see git history) -->
  </div>
*/}
</Layout>

{/*
<style>
  .intro {
    font-size: 1.15rem;
    color: #555;
    margin-bottom: 2rem;
  }
  .contact-card {
    max-width: 550px;
    background: linear-gradient(135deg, #fafbff 0%, #f5f7fa 100%);
    border: 1px solid #e8ecf4;
    border-radius: 16px;
    padding: 2.5rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.04);
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-group label {
    display: block;
    font-weight: 600;
    font-size: 0.9rem;
    color: #333;
    margin-bottom: 0.5rem;
  }

  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: 0.875rem 1rem;
    border: 1.5px solid #dde3ed;
    border-radius: 10px;
    font-family: inherit;
    font-size: 1rem;
    background: #fff;
    transition: all 0.2s ease;
    box-sizing: border-box;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--ucla-blue);
    box-shadow: 0 0 0 3px rgba(39, 116, 174, 0.1);
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder {
    color: #aab4c5;
  }

  .form-group textarea {
    resize: vertical;
    min-height: 140px;
  }

  .submit-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.9rem 1.8rem;
    background: var(--ucla-blue);
    color: white;
    border: none;
    border-radius: 10px;
    font-family: inherit;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .submit-btn:hover {
    background: #1e5f8a;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(39, 116, 174, 0.25);
  }

  .submit-btn svg {
    transition: transform 0.2s ease;
  }

  .submit-btn:hover svg {
    transform: translateX(3px);
  }
</style>
*/}
